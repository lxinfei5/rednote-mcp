# xiaohongshu-mcp

小红书 MCP 服务：用 [Camoufox](https://github.com/daijro/camoufox)（Firefox 反检测构建）做常驻浏览器，经 playwright-go（Juggler 协议）驱动，向 AI 助手暴露小红书的登录初始化、搜索、浏览和用户主页读取能力。只读，不含发布/评论/点赞/收藏/通知等写入。

## 常用命令

| 目的 | 命令 |
| --- | --- |
| 安装 Camoufox + playwright 驱动（首次必做，固定版本 + SHA-256 校验） | `go run ./cmd/camoufox-setup` |
| 启动服务（默认 `127.0.0.1:18060`） | `go run .` |
| 启动服务并显示浏览器窗口 | `go run . -headless=false` |
| 扫码登录（首次必做） | `go run ./cmd/login` |
| 编译二进制 | `go build .` |
| 格式化 | `gofmt -w $(rg --files -g '*.go')` |
| 运行测试 | `go test ./...` |

MCP 端点为 `http://localhost:18060/mcp`。集成测试依赖真实浏览器与登录态，使用 `go test -tags integration ./xiaohongshu` 显式运行。

**供应链约束**：服务运行时绝不自动下载浏览器或驱动。Camoufox 由 `cmd/camoufox-setup`
固定版本、SHA-256 校验后落在 `bin/camoufox`（gitignored）；playwright 驱动（node +
playwright-core）落在 `.playwright-driver`。解析失败一律 fail closed（见
`browser/install.go`、`browser/driver.go`），可用 `XHS_CAMOUFOX_BIN` /
`PLAYWRIGHT_DRIVER_PATH` / `PLAYWRIGHT_NODEJS_PATH` 显式指定。

## 代码结构

- `main.go`、`app_server.go`、`routes.go` —— 进程入口与 HTTP 服务装配
- `mcp_server.go`、`mcp_handlers.go` —— MCP 工具的定义与处理
- `service.go`、`handlers_api.go` —— 业务服务层与 HTTP API
- `browser_session.go` —— 常驻单 Camoufox + 进程内串行 page lease（关 page 不关浏览器；无人为限速）
- `browser/` —— Camoufox 定位/校验（`install.go`）、驱动解析（`driver.go`）、指纹配置（`fingerprint.go`）、playwright-go 引擎（`engine.go`）、cookie 转换（`cookies.go`）
- `humanize/` —— 拟人化鼠标/键盘/滚动输入（基于 playwright Mouse/Keyboard）
- `xiaohongshu/` —— 基于 playwright-go 的读取动作（搜索 / 详情 / 评论读取 / 登录 / 主页）
- `configs/`、`cookies/`、`errors/` —— 配置、登录态、错误

## 开发约定

- 每次改完 Go 代码后执行 `gofmt` 格式化
- 功能改动一律走独立分支；未经同意不推送远程
- 交付顺序：先本地 review，再远程 PR review
- 测试产生的临时脚本与构建中间文件，无用即删
- 不过度设计，保持代码简洁、易读
- 注释用中文，简洁明了，专业名词可用英文
- 错误信息（`error` / `panic`）用英文并遵循 Go 惯例：小写开头、结尾无标点；日志与注释保持中文

## 发版规范

- 发版=打语义化 tag `vX.Y.Z` 推上去（触发 Release），破坏性变更进 major；main 每次推送自动生成的日期 tag `vYYYY.MM.DD.HHMM-sha` 不是发版，别跟它混。

## PR Review 重点

- 反检测由 Camoufox 在 C++ 层完成，**不要再注入 stealth JS 或事后覆盖 UA/Client Hints**（那是 go-rod 时代的做法，已废弃）。
- 指纹/语言/屏幕等经 `CAMOU_CONFIG_<n>` 环境变量传入（见 `browser/fingerprint.go`），新增配置项走这里，不要加 `page.Eval` 注入。
- 读取动作优先用 playwright 原生能力（`page.Evaluate` / `QuerySelector` / `Mouse`），只有 `__INITIAL_STATE__` 数据提取才在页面内 evaluate。

## 工作方法论：先照清全局形状，再动手（Surveyor）

> 正例让你舒服，反例让你正确；先证明想全了，再动手。

**触发判据**：这个决定做错了会不会返工、牵出第二个真源、影响长期演进？会 → 先把问题形状铺清再动手；不会 → 直接做，不杀鸡用牛刀。

**适用**：结构设计 / 架构与方法评估 / 重构 / 分类、路由、目录、决策回路设计 / 多因素复杂问题拆解 / 「该加还是该删、先修哪个」的边界判断。
**不适用**：一句话能定、单点可逆、影响面小的改动；纯执行（命令怎么跑）；空间与主次已被更高层定好的局部填充。

**五件事形状契约（缺一不可）**：

1. **第一性裁决**——先定方向与主次，具名给出四件：**终局目的**（可证伪，能据此拒绝一个无关优化）、**当前主矛盾**（反事实判别器：「只能修一项，修哪项最能改变终局结果」，再追问哪项一旦错会让其它优化整体失效 / 位于因果上游向下游扩散 / 跨会话复发难逆）、**不可交换边界**（单一真源、语义不被实现改写、真实身份不冒充、人的最终裁决权）、**优先级依据**（不预设谁永远第一，现场判断谁最支配结果）。主矛盾通常一个；确属耦合并列或地形客观为平，须逐项验证后具名理由，不许平铺偷懒。
2. **逻辑空间**——证明覆盖、互补、最小。**原子对象**先分类型、不同性质的东西不得偷混在同一格里；**同类型逻辑轴**内先检查完备/互斥，再看轴与轴关系；关系形状分清 **同轴分区 / 正交叠乘 / 约束依赖 / 优先级**（检验：换一轴的值会不会强迫另一轴跟着变？会 → 不是正交，是依赖没拆出来）；**唯一 owner**，禁止靠「其它 / 看情况」藏零命中，显式标 `N/A` / `UNKNOWN+原因` / 未覆盖 residual；每项要有它独有的案例否则即冗余（刻意的备援须显式声明）；标出子项之间的耦合、替代与长期副作用。
3. **反例审查**——主动攻击自己的空间，至少四类各一次：**双命中**（一例同时落两个 owner）、**零命中**（一例落不到任何 owner 被静默省略）、**边界切换**（哪个可观察条件让案例从一类切到另一类，写清了吗）、**失败穿透**（原生能力 / 登录态 / 数据源 / 依赖不可用时，fallback 是否偷偷改写了语义、对象身份或证据等级）。高影响难逆改动可起一个不看现方案的独立视角重推一遍再对照，不以多数票替代最终裁决。
4. **交付形状**——让别人能复核，五件齐全：先**结论 + 当前主矛盾** → **逻辑空间的覆盖/冲突**（哪些轴正交、哪些项约束依赖、谁 owner 谁）→ **先修 / 后修 / 暂不修**（各带干扰与长期代价，暂不修也具名）→ 附**已过的反例攻击清单** → 附 **break_condition 自检结果**。默认从第 1 件进；轴重切或反例翻出新洞就回第 1 件重来；顺序可跳跃，交付时五件缺一不可。
5. **break_condition**——本方法自身的失效信号，命中任一即先重做形状、不进入局部修改：从已有工具/文件倒推分类（而非从问题正推）；平铺所有问题不敢点名主矛盾；同轴混入不同类型对象；双 owner / 零 owner 却不披露；fallback 改写语义却冒充完成；局部优化增加双源或耦合却不计长期代价；只有顺滑正例、没有边界/失败反例。

**三条贯穿立场（防退化）**：

- **整体 > 零件**：先问一个部件该不该存在、属于哪一平面、形态能否从终局目的推出，再谈它的内部深度。局部更优不得换来整体更坏；某平面一句话说清 owner 都做不到，先停下重构问题形状。
- **删 > 加**：复杂度预算为负，合并优于扩张、指针优于复制。新阶段 / 新入口 / 新目录**默认有罪**，只有证明它补上不可复现的地板、或显著减少长期耦合，才允许增加。（绿地/早期项目的主矛盾往往是能力缺失而非复杂度过剩，此时默认有罪的是「无 owner、无法从终局推出的加」，奠基性新增不受此压。）
- **约束形状 ≠ 编排思考**：这是自觉执行的心智形状，不固化成 schema / lint / 强制 gate / 1→2→3→4 硬流水线。一旦做成引擎，就从「帮助想全」退化成「假装想全」。
